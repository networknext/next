DONE

	test_relay_backend_counters

	Extend clean shutdown test to make sure the backend sees the shutting down flag

RELAY

	test_relay_backend_stats

	------------------

	Speed up relay func tests by iterating once per-second until condition is met

	esp. for waiting until initilaize

	------------------

	Debug and fix why the happy path doesn't always work in semaphore

	------------------

	I forgot to implement code that enforces bandwidth envelopes at the relay level.

	... =p

	------------------