version: v1.0

name: Build

agent:
  machine:
    type: e1-standard-8
    os_image: ubuntu1804

blocks:

  - name: "Build on Ubuntu 20.04 LTS"
    dependencies: []
    task:
      prologue:
        commands:
          - checkout
      agent:
        machine:
          type: e1-standard-8
          os_image: ubuntu2004
      jobs:
        - name: build
          commands:
            - gcc --version
            - make -j
            - ./bin/relay version
