HIGH LEVEL

	0. Sort out relay and relay backend stuff and *really* lock it down. No changes to relay and relay <-> relay backend protocols past this point.

	1. Get all analytics stuff pushed to analytics service via pubsub

	2. Insert all analytics stuff into bigquery (emulator)

	3. Get the portal cruncher rebuilt (local)

	4. Get the portal back online in a basic format running locally

	5. Setup terraform scripts to get the dev environment up

	6. Get dev back up with internal addresses working across google and AWS relays (internal groups), plus some bare metal.

	7. Fix IP2Location

	8. Multithreaded relay

	9. Load test, optimize and profile: SDK, relay and backend components.

	10. Document *everything*.

	11. Bring back portal, admin tool, analytics etc... (stretch)