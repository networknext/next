version: v1.0
name: Production Deploy
agent:
  machine:
    type: e1-standard-8
    os_image: ubuntu1804
global_job_config:
  prologue:
    commands:
      - sudo apt-get update && sudo apt-get -y install libsodium-dev libzmq3-dev
      - gcloud auth activate-service-account --key-file=/home/semaphore/gcp-service-account.json
      - gcloud auth configure-docker -q
      - checkout
      - sem-version go 1.13
      - make clean
blocks:
  - name: Build & Publish Artifacts
    dependencies: []
    task:
      jobs:
        - name: Billing
          commands:
            - make build-billing-prod-artifact
            - make publish-billing-prod-artifact
        - name: Portal
          commands:
            - make build-portal-prod-artifact
            - make publish-portal-prod-artifact
        - name: Portal Cruncher
          commands:
            - make build-portal-cruncher-prod-artifact
            - make publish-portal-cruncher-prod-artifact
        - name: Relay Backend
          commands:
            - make build-relay-backend-prod-artifact
            - make publish-relay-backend-prod-artifact
        - name: Server Backend
          commands:
            - make build-server-backend-prod-artifact
            - make publish-server-backend-prod-artifact
        - name: Bootstrap Script
          commands:
            - make publish-bootstrap-script-prod
      secrets:
        - name: google-cloud-platform-prod
