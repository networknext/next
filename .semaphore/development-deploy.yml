version: v1.0
name: Development Deploy
agent:
  machine:
    type: e1-standard-8
    os_image: ubuntu1804
global_job_config:
  prologue:
    commands:
      - sudo apt-get update && sudo apt-get -y install libsodium-dev
      - gcloud auth activate-service-account --key-file=/home/semaphore/gcp-service-account.json
      - gcloud auth configure-docker -q
      - checkout
      - sem-version go 1.13
      - make clean
blocks:
  - name: Build & Publish Artifacts
    dependencies: []
    task:
      jobs:
        - name: Billing
          commands:
            - make build-billing-artifact
            - make publish-billing-artifact
        - name: Portal
          commands:
            - make build-portal-artifact
            - make publish-portal-artifact
        - name: Portal Cruncher
          commands:
            - make build-portal-cruncher-artifact
            - make publish-portal-cruncher-artifact
        - name: Relay Backend
          commands:
            - make build-relay-backend-artifact
            - make publish-relay-backend-artifact
        - name: Server Backend
          commands:
            - make build-server-backend-artifact
            - make publish-server-backend-artifact
        - name: Bootstrap Script
          commands:
            - make publish-bootstrap-script
      secrets:
        - name: google-cloud-platform
