DONE

	Fix instance of ~secrets/* -> ~/secrets/* in docs

	Make apt run in non-interactive mode in terraform scripts via DEBIAN_FRONTEND=noninteractive

	Update maxmind.com ip2location download to the new method: https://dev.maxmind.com/geoip/updating-databases#directly-downloading-databases

	Add newline check to terraform-akamai.txt

	Add newline check to terraform-cloudflare.txt

	Don't rely on default google networks in terraform. Some orgs don't allow creation of default networks: https://www.googlecloudcommunity.com/gc/Infrastructure-Compute-Storage/Is-there-a-default-netwok-in-a-new-provisioned-project/m-p/428917

TODO

	Fork the repo and run through the setup process via mas-bandwidth.com github account from scratch

	Create a new 1.0.1 release with fixes
